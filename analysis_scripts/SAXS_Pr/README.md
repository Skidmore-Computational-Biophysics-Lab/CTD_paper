The CA_distances.sh script uses the .ptraj scripts in this folder to calculate the distance between each CA atom in the CTD peptide. Duplicate distances are removed within the Jupyter Notebooks.
Since adjacent CA atoms have a common distance of ~3.8 Angstroms, these distances have been removed using move_adj.sh
